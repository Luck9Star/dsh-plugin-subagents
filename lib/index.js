// dsh-plugin-subagents — minimal scaffold stub.
// Real plugin logic lands in later tasks; this exists so `npm test` and
// `npm run lint` have something to exercise and load.
export const name = 'dsh-plugin-subagents'

export function apply() {}
